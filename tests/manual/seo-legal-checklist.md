# SEO And Legal Checklist

- [x] The page exposes one `h1` and section-level `h2` headings.
- [x] The homepage head includes `title`, `meta description`, Open Graph metadata, and `canonical`.
- [x] All primary navigation and CTA links include descriptive `title` attributes.
- [x] `privacidad.html` loads correctly from the footer.
- [x] `aviso-legal.html` loads correctly from the footer.
- [x] The Open Graph image asset resolves successfully.